{
  "name": "vite-react-typescript-starter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "rm -rf dist && vite build && cp core-files/manifest.json dist/ && cp -r core-files/icons dist/ && cp -r core-files/images dist/",
    "preview": "vite preview",
    "export": "node create-zip.js --experimental-modules"
  },
  "dependencies": {
    "webextension-polyfill": "^0.10.0",
    "archiver": "^5.3.1"
  },
  "devDependencies": {
    "vite": "^5.4.2",
    "@types/node": "^20.11.24",
    "autoprefixer": "^10.4.18",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.1"
  }
}
